# No environment variables needed for the client.
#
# API routing is handled by vercel.json rewrites:
#   /api/* → http://your-eb-url.elasticbeanstalk.com/api/*
#
# Before deploying to Vercel, update the destination URL in vercel.json
# to your actual Elastic Beanstalk environment URL.
